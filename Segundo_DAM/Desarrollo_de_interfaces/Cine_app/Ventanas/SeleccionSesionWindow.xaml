<Window x:Class="Cine_app.Views.SeleccionSesionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        Title="Seleccionar Sesión" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style x:Key="SesionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e94560"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header con info de película -->
        <Border Grid.Row="0" Background="#1a1a2e" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Imagen pequeña de la película -->
                <Border Width="80" Height="120" 
                       Background="#ddd"
                       CornerRadius="5"
                       Margin="0,0,20,0">
                    <Image x:Name="imgPelicula" 
                          Stretch="UniformToFill"/>
                </Border>

                <!-- Info de la película -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="txtTitulo" 
                              FontSize="24" 
                              FontWeight="Bold"
                              Foreground="White"/>
                    <TextBlock x:Name="txtInfo" 
                              FontSize="14" 
                              Foreground="#ccc"
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Botón cerrar -->
                <Button Grid.Column="2" 
                       Content="✕" 
                       Click="BtnCerrar_Click"
                       Background="Transparent"
                       Foreground="White"
                       BorderThickness="0"
                       FontSize="24"
                       Width="40"
                       Height="40"
                       Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Contenido principal -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Calendario -->
            <Border Grid.Column="0" 
                   Background="White" 
                   CornerRadius="10" 
                   Padding="20"
                   Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Selecciona una fecha" 
                              FontSize="18" 
                              FontWeight="Bold"
                              Margin="0,0,0,15"/>

                    <Calendar x:Name="calendario"
                             SelectedDatesChanged="Calendario_SelectedDatesChanged"
                             HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Panel de sesiones -->
            <Border Grid.Column="1" 
                   Background="White" 
                   CornerRadius="10" 
                   Padding="20"
                   Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Título -->
                    <TextBlock Grid.Row="0" 
                              Text="Horarios disponibles" 
                              FontSize="18" 
                              FontWeight="Bold"
                              Margin="0,0,0,15"/>

                    <!-- Loading -->
                    <StackPanel x:Name="pnlLoadingSesiones" 
                               Grid.Row="1"
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center"
                               Visibility="Collapsed">
                        <TextBlock Text="⏳" FontSize="40" HorizontalAlignment="Center"/>
                        <TextBlock Text="Cargando sesiones..." 
                                  FontSize="14" 
                                  Foreground="#666"
                                  Margin="0,10,0,0"/>
                    </StackPanel>

                    <!-- Sin sesiones -->
                    <StackPanel x:Name="pnlSinSesiones" 
                               Grid.Row="1"
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center"
                               Visibility="Collapsed">
                        <TextBlock Text="📅" FontSize="40" HorizontalAlignment="Center" Opacity="0.3"/>
                        <TextBlock Text="No hay sesiones disponibles" 
                                  FontSize="14" 
                                  Foreground="#666"
                                  TextAlignment="Center"
                                  Margin="0,10,0,0"/>
                        <TextBlock Text="para esta fecha" 
                                  FontSize="14" 
                                  Foreground="#666"
                                  TextAlignment="Center"/>
                    </StackPanel>

                    <!-- Lista de sesiones -->
                    <ScrollViewer Grid.Row="1" 
                                 x:Name="scrollSesiones"
                                 VerticalScrollBarVisibility="Auto"
                                 Visibility="Collapsed">
                        <ItemsControl x:Name="itemsSesiones">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#f9f9f9" 
                                           CornerRadius="8" 
                                           Padding="15" 
                                           Margin="0,0,0,10"
                                           BorderBrush="#e0e0e0"
                                           BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel>
                                                <TextBlock FontSize="20" FontWeight="Bold" Foreground="#1a1a2e">
                                                    <Run Text="🕐 "/>
                                                    <Run Text="{Binding FechaHora, StringFormat='{}{0:HH:mm}'}"/>
                                                </TextBlock>
                                                <TextBlock FontSize="14" Foreground="#666" Margin="0,5,0,0">
                                                    <Run Text="Sala: "/>
                                                    <Run Text="{Binding Sala.Nombre}" FontWeight="SemiBold"/>
                                                </TextBlock>
                                                <TextBlock FontSize="16" Foreground="#e94560" FontWeight="Bold" Margin="0,5,0,0">
                                                    <Run Text="{Binding Precio, StringFormat='{}{0:C}'}"/>
                                                    <Run Text=" / butaca" FontSize="12" FontWeight="Normal"/>
                                                </TextBlock>
                                            </StackPanel>

                                            <Button Grid.Column="1"
                                                   Content="Seleccionar ➔"
                                                   Style="{StaticResource SesionButtonStyle}"
                                                   Click="BtnSeleccionarSesion_Click"
                                                   Tag="{Binding}"
                                                   VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>